PROGRAM EXEMPLO2;
USES CRT;
VAR X:INTEGER;
PROCEDURE VERIFICA(Y:INTEGER);
BEGIN
IF(Y>=0)THEN
WRITELN('NUMERO POSITIVO')
ELSE WRITELN('NUMERO NEGATIVO');
END;
BEGIN
WRITELN('DIGITE UM NUMERO');
READLN(X);
VERIFICA(X);
READKEY;
END.